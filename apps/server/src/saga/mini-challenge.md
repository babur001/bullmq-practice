1. First once shipping fails -> start compensating. Running the saga-machine in reverse order now:
   - Un-authorize the payment service, or leave it for its ttl. It is easy cause no chargin happened so no refund step here
   - Set sale as cancelled with failed reason, maybe in future we might investigate failure reason
2. idempotency + retry mechanism (here the job is still persisted in disk so if attempts exhausted and still no return here we might add additional security like sale -> still in payment_refund step but its status is more than expected we might re-trigger it to compensation saga again, still shout and log it in some table and notify the admins who migth handle this)
3. Of course it might fail, since it is uses the exact same service that also might have failed in forward step. Downtime, server is down, authorization fail if there is some guard between them, updated or deprecated API that we were still using but later turned off, a lot of reasons we might not notice. No idempotent key between them might fire refund twice
4. If we have validation before even sale flow is started, whether we ship to current user selected region, warehouse etc, then yes maybe putting before shipping would be eligible. But all that validation happend inside the service itself, or maybe validation passed once later plans changed and we no longer ship there might fail, then yes it would cause hard to compensate step act first. Putting ship before validates that capture is safe and make the action just transient, more retries -> success
