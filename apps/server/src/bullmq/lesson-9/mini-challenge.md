1. the jobId. On next iteration of relay, the outbox row is re-run again, but no op, since bullmq deduplicates jobs with same id
2. It is not some bug that might cause some problems, but just overrunning of same jobs more than once. Here we should lock the selected rows, and itself also should not wait before runned transactions locked rows with skip locked
3. I think we should implement here flow. First payment -> warehouse -> shipping -> notification. If bullmq provides it, we can implement it, but same issue orphaned jobs or pending row in db. Let me guess... After payment, maybe adding to next queue in the order (warehouse here) and follow it. But same issue what if payment charged bu downstream job failed... Here i think we need more outbox rows but with different topics, but its status should be linked with prev outbox row but it makes the querying and imlementation more complex... I got an idea, when setting publish:true, how about setting the next outbox row with tx in one atomic transaction, both updating and inserting next outbox topic which will be handled by relay in next iteration.. But here i must be here very careful when structuring the next outbound insertion steps, since setting first shipping before payment is wrong, or setting notification before all.. Maybe for every flow like here sale flow, i will implement a data-structure where by current outbox topic will get next() topic title. So that data structure is probably a linkedin list for accessing next() and prev() topics.
4. event row, since all are unique, and easy to debug where the relay is currently on
